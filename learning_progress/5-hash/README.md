# Simple hash generator
Uses SHA256 hashing algorithm to create a hash of the word hello, this will then be implemented on the syscall table so as to get a hashed version of the live syscall table which will be compared 
with static version from the system.map
